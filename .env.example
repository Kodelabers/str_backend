# --- Database ---
PROD_DB_USERNAME=shorttermrental
PROD_DB_PASSWORD=strrn26!

# --- Mail relay (Infodom S-SMTP-01 or similar) ---
SPRING_MAIL_HOST=localhost
SPRING_MAIL_PORT=25
APP_MAIL_FROM=eturizam@mints.hr

# --- Draft encryption key (AES-256, base64-encoded 32 bytes). Generate with:
#     openssl rand -base64 32
DRAFT_ENC_KEY=CHANGE_ME_base64_32_bytes
